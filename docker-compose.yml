version: "3.9"
services:
  todo-app:
    build: .
    container_name: todo_flask_app

    ports:
      - "5000:5000"

    volumes:
      - .:/app

    restart: always

    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
